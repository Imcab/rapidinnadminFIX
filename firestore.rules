rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Acceso solo para usuarios autenticados (anónimos incluidos)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
